fastapi>=0.115.0
python-jose[cryptography]>=3.3.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20
httpx>=0.27.0
# collector 패키지가 쓰는 유일한 외부 의존.
# 앱은 httpx(비동기)를 쓰지만 수집기는 스택 없이 도는 동기 코드라 requests 를 쓴다.
# ⚠️ pykrx · FinanceDataReader 는 넣지 않는다 — 둘 다 robots.txt 로 막힌 경로를
#    타므로 사용하지 않기로 했다(#23 · #33).
requests>=2.32.0
redis[asyncio]>=5.2.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
bcrypt>=4.2.0
python-dotenv>=1.0.1
pydantic[email]>=2.10.0
pydantic-settings>=2.7.0
qdrant-client>=1.12.0
beautifulsoup4>=4.12.0
pandas>=2.0.0
numpy>=1.26.0
lightgbm>=4.3.0
scikit-learn>=1.4.0

# LangChain / LangGraph
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
langchain-qdrant>=0.2.0
langgraph>=0.2.0

# Neo4j
neo4j>=5.14.0

# Celery (비동기 태스크 큐 – broker/backend: Redis)
celery[redis]>=5.4.0

# Document parsing (PPT, DOC, XLS, PDF)
python-pptx>=1.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
pdfplumber>=0.11.0
Pillow>=10.0.0
